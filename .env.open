# Open/Public Feed Configuration
# For feeds that don't require authentication

# Dummy value to pass validation (not actually used)
API_KEY=public_feed

# Transit agency name
PROVIDER=PublicTransit

# Public feed URL (no API key required)
FEED_URL=https://example.com/gtfs-realtime.pb

# Disable API key injection since this feed doesn't use it
API_KEY_HEADER=
API_KEY_QUERY=

# Optional S3 storage
S3_BUCKET=public-transit-data

# Log file
LOG_FILE=./logs/public.log
